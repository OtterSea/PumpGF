# 框架初始化空项目.md

> **本文档用途**：新建一个引入了 PumpGF 框架的空项目后，让 AI 执行本文档，在项目的**正确位置**生成各 AI 工具的导航文件，使后续每个 AI 新会话都能自动读到框架规范与项目宪章，无需手动 @。
> **执行方**：AI 编程助手（收到本文档后按此执行）。**人类**只需：开新项目 → 引入 PumpGF → 把本文档交给 AI → 说"按《框架初始化空项目.md》执行初始化"。
> **性质**：一次性初始化动作。可重复执行（增量，不覆盖已有内容）。

---

## 〇、为什么要做这一步（背景）

PumpGF 的框架规范文档（宪章、框架说明、工作流等）全部位于 `Packages/PumpGF/AI编程规范/` 内。但不同 AI 工具的自动读取机制不同，**框架规范不会因为它们"在框架里"就被自动读到**：

| AI 工具 | 自动读取的机制 | 结论 |
|---------|--------------|------|
| **Kilo** | 项目根 `AGENTS.md`，新会话自动读 | 在项目根放 `AGENTS.md` 即生效 |
| **Codely CLI** | 项目根 `CODELY.md`（+ global 作用域），新会话自动读；**不会扫子目录** | 在项目根放 `CODELY.md` 即生效；在 `Packages/PumpGF/CODELY.md` 放文件也**不生效** |
| **CodeBuddy** | **没有自动读取的约定文件**；只注入 git status + 文件结构快照 | 需靠 Setting 必读文本段，或手动说"先读 PROJECT_NOTES.md" |

因此，初始化要在项目根生成导航文件，作为"指针"指向框架内的规范。这样框架规范跟着 PumpGF 走，任何项目引入后，AI 都能顺着项目根指针找到。

---

## 一、执行步骤（AI 按此操作）

### 步骤 1：确认框架与项目现状

- 确认 `Packages/PumpGF/` 存在（框架已引入）。
- 用 `git status` / 目录列举确认项目根当前已有哪些文件（尤其 `AGENTS.md`、`CODELY.md`、`PROJECT_NOTES.md`、`.codebuddy/`）。

### 步骤 2：在项目根生成 4 份导航文件（增量，不覆盖已有内容）

对以下每个文件：**若已存在，只在其开头追加"项目导航"区块，绝不改动原有内容；若不存在，完整创建。** 创建/追加前先 Read 确认真实内容。

#### ① `AGENTS.md`（给 Kilo，项目根）——自动读

> 若已存在：在文件开头插入下面的导航区块，保留原内容。

```markdown
## 项目导航（Kilo 会话开始必读）

本项目是 PumpGF 框架 + 游戏业务的组合。会话开始时请按需读取：

1. `Packages/PumpGF/AI编程规范/constitution.md` —— 框架宪法（AI 编码铁律）
2. `Packages/PumpGF/AI编程规范/框架说明.md` —— 框架能力路由表与模块清单
3. `Packages/PumpGF/AI编程规范/开发编程规范.md` —— 代码规范
4. `项目宪章.md` —— 本项目愿景/架构/规范/进度统一入口（若存在，见步骤 3）
5. `Packages/PumpGF/框架使用说明书.md` —— 框架使用说明（首次可读）

> 框架规范正文在 `Packages/PumpGF/AI编程规范/` 内，本文件只是导航指针，不承载规范内容。
```

#### ② `CODELY.md`（给 Codely CLI，项目根）——自动读

> ⚠️ **关键约束**：Codely CLI 的 `CODELY.md` 内含 `## Codely Structured Memories` 结构化记忆区，由记忆工具（`append_memory`/`update_memory`）管理，**绝不能手动改**。导航只能加在该区**之外**（正文区，通常是文件开头或末尾），且不要删改记忆区的任何内容。

> 若 `CODELY.md` 已存在：Read 全文，找到 `## Codely Structured Memories` 区，把导航区块加在**该区之外**（建议加到文件最开头，或用分隔线隔开）。若不存在：直接创建，含导航区块。

```markdown
## 项目导航（Codely 会话开始必读）

本项目是 PumpGF 框架 + 游戏业务的组合。会话开始时请按需读取：

1. `Packages/PumpGF/AI编程规范/constitution.md` —— 框架宪法（AI 编码铁律）
2. `Packages/PumpGF/AI编程规范/框架说明.md` —— 框架能力路由表与模块清单
3. `Packages/PumpGF/AI编程规范/开发编程规范.md` —— 代码规范
4. `项目宪章.md` —— 本项目愿景/架构/规范/进度统一入口（若存在，见步骤 3）
5. `Packages/PumpGF/框架使用说明书.md` —— 框架使用说明（首次可读）

> 框架规范正文在 `Packages/PumpGF/AI编程规范/` 内，本文件只是导航指针，不承载规范内容。
```

#### ③ `PROJECT_NOTES.md`（给 CodeBuddy，项目根）——**不自动读，需 Setting 或手动说"先读它"**

> 生成该文件作为"可被搜到的项目说明"。但**明确标注**：CodeBuddy 不会自动读它。需用户在 CodeBuddy Setting 中加必读文本段（见附录），或每次会话手动说"先读 PROJECT_NOTES.md"。

```markdown
## 项目导航（CodeBuddy 必读，需手动触发）

> ⚠️ **CodeBuddy 不会自动读取本文件**。需在 CodeBuddy 的 Setting（自定义指令/记忆）中添加必读文本段（见《框架初始化空项目.md》附录），或每次会话手动说"先读 PROJECT_NOTES.md"。

本项目是 PumpGF 框架 + 游戏业务的组合。会话开始时请按需读取：

1. `Packages/PumpGF/AI编程规范/constitution.md` —— 框架宪法（AI 编码铁律）
2. `Packages/PumpGF/AI编程规范/框架说明.md` —— 框架能力路由表与模块清单
3. `Packages/PumpGF/AI编程规范/开发编程规范.md` —— 代码规范
4. `项目宪章.md` —— 本项目愿景/架构/规范/进度统一入口（若存在，见步骤 3）
5. `Packages/PumpGF/框架使用说明书.md` —— 框架使用说明（首次可读）
```

#### ④（可选）`项目宪章.md`（项目根）

> 若项目已有宪章/文档（如 `Assets/_ProjectDocs/` 下有 `constitution.md` 或类似），**不要重复创建**，只需在步骤 2 的导航指针里把"项目宪章.md"改为实际路径。若项目尚无宪章，则创建一份 `项目宪章.md`，内容让 AI 与用户协商（项目愿景、平台、核心架构决策、当前进度）。

### 步骤 3：核对并汇报

- 逐项 Read 确认生成的导航文件内容正确、未破坏原有内容。
- **CodeBuddy 用户的 Setting 文本段**：告知用户去 CodeBuddy 的 Setting（自定义指令/记忆）粘贴附录里的必读文本段。这一步 AI 无法代劳，需用户手动。

---

## 二、附录：CodeBuddy 的 Setting 必读文本段

因为 CodeBuddy **不会自动读项目根的说明文件**，使用 CodeBuddy 的用户需在其 **Setting → 自定义指令 / Memory**（或类似"系统提示"/"每次会话附加"的入口）中添加以下文本段，让每次新会话都能被引导去读导航：

```
本项目是 PumpGF 框架 + Unity 游戏项目。每次会话开始时，请先读取项目根目录的 PROJECT_NOTES.md（若存在），它会指引你读取框架规范（Packages/PumpGF/AI编程规范/ 下的 constitution.md、框架说明.md、开发编程规范.md）与项目宪章。若有 SPEC 相关需求，遵循 Packages/PumpGF/AI编程规范/speckit/ 的工作流。
```

> 若 CodeBuddy 支持"全局规则"（`.codebuddy/rules/` 等），也可以把上述文本放进去。但需先实测该机制是否真的在会话开头生效——CodeBuddy 官方未确证这一点，实测为准。

---

## 三、执行完成后的验收

| 检查项 | 通过标准 |
|--------|---------|
| `AGENTS.md`（Kilo） | 项目根存在，导航区块已写入，原内容未被破坏 |
| `CODELY.md`（Codely） | 项目根存在，导航加在记忆区之外，`## Codely Structured Memories` 区原样未动 |
| `PROJECT_NOTES.md`（CodeBuddy） | 项目根存在，本文件标注了"需手动触发" |
| `项目宪章.md` | 存在（若新建），内容由 AI + 用户协商确认 |
| CodeBuddy Setting | 已告知用户粘贴附录文本段（AI 无法代劳，需用户手动） |
| 框架文档路径 | 导航指针里的框架文档路径真实存在（点进去能读到） |

---

## 四、重复执行说明

本初始化是**增量**的，可重复执行：
- 导航文件已存在且包含导航区块 → 跳过，不重复追加。
- 导航文件已存在但缺导航区块 → 只补充缺失的区块。
- 框架升级导致规范文档路径变化 → 重新执行，更新指针里的路径。

---

*本文件随 PumpGF 框架分发。新建空项目、引入 PumpGF 后，请让 AI 执行本文件完成初始化。*
